/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['fakestoreapi.com'], // Adiciona o domínio de onde as imagens estão sendo carregadas
    },
  };
  
  module.exports = nextConfig;
  